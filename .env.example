# Required in production
SECRET_KEY=change-me-to-a-long-random-string
HOST_PASSWORD=choose-a-strong-host-password

# Public base URL for share links (no trailing slash)
APP_BASE_URL=https://simple-lists.example.com

# SQLite path (default shown)
# DATABASE_URL=sqlite:////data/simple_lists.db

# Set to 1 only for local development with placeholder secrets
# SL_ALLOW_DEV_SECRET=1
